<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
     xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
     xmlns:aop="http://www.springframework.org/schema/aop"
     xmlns:tx="http://www.springframework.org/schema/tx"
     xsi:schemaLocation="
     http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-2.5.xsd
     http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-2.5.xsd
     http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop-2.5.xsd">
     
	<!--  import resources from OG-TimeSeriesStorage -->
  	<import resource="classpath:com/opengamma/timeseries/db/tssQueries.xml" />

	<bean id="propertyConfigurer"
		class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
		<property name="location"><value>file:tests.properties</value></property>
	</bean>
	
	<bean id="derbyDataSource" class="org.apache.commons.dbcp.BasicDataSource" destroy-method="close">
		<property name="driverClassName" value="${derby.jdbc.driver}"/>
		<property name="url" value="${derby.jdbc.url}/test_${user.name}"/>
		<property name="username" value="${derby.jdbc.username}"/>
		<property name="password" value="${derby.jdbc.password}"/>
	</bean>
	
	<!-- The postgres PlatformTransactionManager -->
  	<bean id="postgresTxManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
  		<property name="dataSource" ref="postgresDataSource"/>
  	</bean>
  	
  	<!-- The postgres PlatformTransactionManager -->
  	<bean id="derbyTxManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
  		<property name="dataSource" ref="derbyDataSource"/>
  	</bean>
	
	<bean id="derbyDao" class="com.opengamma.timeseries.db.DerbyTimeSeriesMaster">
		<constructor-arg ref="derbyTxManager"/>
		<constructor-arg ref="tssNamedSQLMap"/>
	</bean>
	
	<bean id="postgresDataSource" class="org.apache.commons.dbcp.BasicDataSource" destroy-method="close">
		<property name="driverClassName" value="${postgres.jdbc.driver}"/>
		<property name="url" value="${postgres.jdbc.url}/test_${user.name}?protocolVersion=2"/>
		 <!-- 
		 <property name="url" value="${postgres.jdbc.url}/test_${user.name}"/>
		  -->
		<property name="username" value="${postgres.jdbc.username}"/>
		<property name="password" value="${postgres.jdbc.password}"/>
	</bean>
	
	<bean id="postgresDao" class="com.opengamma.timeseries.db.PostgresTimeSeriesMaster">
		<constructor-arg ref="postgresTxManager"/>
		<constructor-arg ref="tssNamedSQLMap"/>
	</bean>
	
	<!-- enable the configuration of transactional behavior based on annotations -->
  	<tx:annotation-driven transaction-manager="derbyTxManager"/>
  	<tx:annotation-driven transaction-manager="postgresTxManager"/>
	
</beans>
