# test comment line
; test comment line
# additional blank line below

[global]
property = two

[one]
factory = com.opengamma.component.MockComponentFactoryOne
# test comment line
; test comment line
# additional blank line below

alpha = ${property}
beta = b
gamma = ${property.not.present?}

[two]
factory = com.opengamma.component.MockComponentFactoryTwo
delta = ::one
epsilon = ::three?
info = MockComponent::one
list = Alpha,Beta,Gamma
