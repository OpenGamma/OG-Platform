<?xml version="1.0" encoding="UTF-8"?>
<project
    xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.opengamma</groupId>
    <artifactId>og-parent</artifactId>
    <version>1.2.0-SNAPSHOT</version>
    <relativePath>../..</relativePath>
  </parent>
  <artifactId>og-web</artifactId>
  <packaging>jar</packaging>
  <name>OG-Web</name>
  <description>OpenGamma platform website</description>

  <scm>
    <url>https://github.com/OpenGamma/OG-Platform/tree/master/projects/OG-Web</url>
  </scm>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-antrun-plugin</artifactId>
        <executions>
          <execution>
            <phase>package</phase>
            <configuration>
              <target>
                <mkdir dir="${dist.dir}"/>
                <zip basedir="web-engine" destfile="${dist.dir}/web-engine.zip" />
                <zip basedir="web-push" destfile="${dist.dir}/web-push.zip" />
                <zip basedir="web-marketdata" destfile="${dist.dir}/web-marketdata.zip" />
                <zip basedir="web-sharedmasters" destfile="${dist.dir}/web-sharedmasters.zip" />
              </target>
            </configuration>
            <goals>
              <goal>run</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>build-helper-maven-plugin</artifactId>
        <executions>
          <execution>
            <id>attach-artifacts</id>
            <phase>package</phase>
            <goals>
              <goal>attach-artifact</goal>
            </goals>
            <configuration>
              <artifacts>
                <artifact>
                  <file>${dist.dir}/web-engine.zip</file>
                  <type>zip</type>
                  <classifier>web-engine</classifier>
                </artifact>
                <artifact>
                  <file>${dist.dir}/web-push.zip</file>
                  <type>zip</type>
                  <classifier>web-push</classifier>
                </artifact>
                <artifact>
                  <file>${dist.dir}/web-marketdata.zip</file>
                  <type>zip</type>
                  <classifier>web-marketdata</classifier>
                </artifact>
                <artifact>
                  <file>${dist.dir}/web-sharedmasters.zip</file>
                  <type>zip</type>
                  <classifier>web-sharedmasters</classifier>
                </artifact>
              </artifacts>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

  <dependencies>
    <dependency>
      <groupId>com.opengamma</groupId>
      <artifactId>og-financial</artifactId>
      <version>${project.version}</version>
    </dependency>

    <dependency>
      <groupId>org.eclipse.jetty</groupId>
      <artifactId>jetty-webapp</artifactId>
    </dependency>
    <dependency>
      <groupId>org.cometd.java</groupId>
      <artifactId>cometd-java-server</artifactId>
    </dependency>

    <dependency>
      <groupId>org.testng</groupId>
      <artifactId>testng</artifactId>
      <scope>provided</scope>
    </dependency>
  </dependencies>

  <properties>
    <og-platform.basedir>${project.parent.basedir}</og-platform.basedir>
    <dist.dir>${project.basedir}/dist</dist.dir>
  </properties>

</project>
