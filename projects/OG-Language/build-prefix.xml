<project name="build-prefix">

	<property environment="env" />
	<property file="${user.home}/.OG-Build-common.properties" />
	<property file="${basedir}/build.properties" />
	<property file="${common.dir}/build.properties" />

	<!--
	     Paths required:

		java.build.path		Pure Java sub-projects to be built in the solution
		cc.build.path		Pure C/C++ sub-projects to be built in the solution
		javatest.build.path	Java sub-projects containing tests to be built in the solution
		cctest.build.path	C/C++ test sub-projects to be built in the solution
	-->

</project>
