# This file will be included AFTER the environment is loaded
# and ${user.name}.build.properties has been included.
og-engine.version=0.1

build.debug=true
build.optimize=false
build.verbose=false
build.listfiles=false
build.src=1.6
build.target=1.6
jar.compress=false

src.build.output=${basedir}/classes
tests.build.output=${basedir}/tests/classes
tests.junit.output=${basedir}/tests/output
lib.dir=${basedir}/lib
dist.dir=${basedir}/dist

# necessary for ivy
ivy.install.version=2.0.0
ivy.jar.dir=${basedir}/ivy
ivy.jar.file=${ivy.jar.dir}/ivy.jar
ivy.settings.file=${basedir}/ivysettings.xml
build.dir=${src.build.output}
src.dir=src