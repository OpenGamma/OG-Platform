<project name="installer" default="build">

  <target name="build-servers" unless="disable.servers">
    <subant buildpath="." antfile="servers.xml" inheritall="true" target="build" />
  </target>

  <!-- TODO: build the R bundle that contains both OG-Language and OG-Rstats, with/without JVM -->

  <!-- TODO: build a monolithic MSIs comprising all the above (and any other MSIs from the checkout) -->
  
  <target name="build" depends="build-servers" description="Builds the installer packages in the dist sub-directory" />

</project>
